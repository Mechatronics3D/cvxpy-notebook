# cvxpy-notebook
Jupyter Notebook for CVXPY includes:
   - cvxpy
   - cvxflow

## Usage
You can run the container like this:

`docker run -p 8888:8888 mechatronics3d/cvxpy-notebook`

Then go to this address on your browser:

`http://localhost:8888/`
